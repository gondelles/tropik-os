# 90_AI_SYSTEM

Private reusable infrastructure for AI-assisted operations.

Current components:
- `06_AI_COLLABORATION_PROTOCOL.md` — governance.
- `handoffs/` — structured AI/operator task contracts.
- `prompts/` — platform/project instructions.

Future folders may include `skills/` and `schemas/` only when repeated workflows justify them.

Do not create agent proliferation before a manual workflow proves useful.
