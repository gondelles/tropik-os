# 03_GOLDEN_MASTER_PARITY_MATRIX

**Status:** PRIVATE_WORKING
**Purpose:** one row per surface/component the WordPress build must match, naming exactly what "matches" means, at which breakpoints, verified how, and its current status. This is the working checklist `09_IMPLEMENTATION_PLAN_4_ITERATIONS.md`'s iteration reviews are scored against.

**Golden Master IA note (binding for every row below):** per the Golden Master's own `README.md`, its frozen screenshots/source still show the DEC-026 drawer grouping (`Explore Re:Build` / `Support` / `Project` / `Your Tropik` / `Design reference`). Every navigation-related row in this matrix targets the **2026-09-07 synchronized IA** (`Explore Re:Build` / `Support` absorbing About·Updates·FAQ·Contact·Transparency·Work With Us·Shipping·Terms·Privacy·Returns / `Shop` as its own heading with `Merch`·`Products` / `My Account`·Orders·Bag, no `Project` group) from `RB_WEB_UX_NAVIGATION_COLLECTIONS_EDITABILITY_OVERRIDES_v0.1.md`, not the literal link list visible in the frozen screenshots.

---

| # | Surface / component | Artifact requirement | WordPress target | Breakpoints | Verification method | Status |
|---|---|---|---|---|---|---|
| 1 | Design tokens | `TOKENS.md` verbatim — every hex/value, esp. Coral `#F25B66`; `Plus Jakarta Sans` actually enqueued (not just CSS-referenced) | `theme.json` + global stylesheet custom properties, 1:1 with `TOKENS.md` | N/A (tokens, not layout) | Diff token values against `TOKENS.md` line-by-line | PENDING — not started |
| 2 | Header (desktop) | `[hamburger]—[centered toucan]—[ES/EN pill][search][account][bag+count]`, `.tk-header` 72px, sticky, translucent+blur | Same, WordPress custom header template part | 1440, 1024 | Screenshot vs. Golden Master; no isolated header screenshot exists yet in Golden Master — capture during Iteration 1 review | **BLOCKED — Golden Master has no isolated header capture; must derive from full-page Home screenshot + `COMPONENT_INVENTORY.md` spec** |
| 3 | Header (mobile) | `[hamburger]—[centered toucan]—[search][account][bag]`, 60px, language lower-priority | Same | 768, 480, 390 | Same as #2 | Same blocker as #2 |
| 4 | Left drawer — structure | 2026-09-07 IA (see note above), `.tk-drawer__cols` 2-col ≥560px / 1-col below, closes on X/scrim/ESC/**link tap** | Custom drawer template, not `wp_nav_menu()` alone | 1440, 1024, 768, 480, 390 | Golden Master has **no drawer-open screenshot at any width** | **BLOCKED — must build from `COMPONENT_INVENTORY.md` + `INTERACTIONS.md` #1 spec only, verify visually once WordPress build exists** |
| 5 | Left drawer — close-on-navigate | Not yet true even in the frozen Artifact (`TkPanel` wires only backdrop + explicit close button) | Must be true in WordPress even though it isn't yet true in the reference — this is a founder-directed **improvement over** the frozen source, not parity with it | N/A | Functional test: tap any drawer link, confirm drawer closes | PENDING — explicitly a "build better than the freeze" item, flag in Iteration 1 review |
| 6 | Right Bag drawer | ONE `.tk-cart-panel`, Woo-backed AJAX, populated layout (thumbnail/title/variation/price/qty stepper/remove/subtotal/View bag/Checkout), empty state, `min(92vw,400px)` width cap | Custom cart-fragment-driven drawer, no nested Woo Mini-Cart block | 1440, 1024, 768, 480, 390, populated + empty | Golden Master has **no bag screenshot, populated or empty, at any width** | **BLOCKED — build from `INTERACTIONS.md` #1 + `COMPONENT_INVENTORY.md` "Cards" bag-line spec + `RB_WORDPRESS_WOOCOMMERCE_ARCHITECTURE_v0.1.md`, verify once functional** |
| 7 | Card rail (general) — desktop/tablet | `.tk-carousel`, scroll-snap track, prev/next page-scroll buttons | Same pattern, reused sitewide | 1440, 1024, 768 | Visible in Home screenshot (Materials section) | READY — pattern exists and is screenshotted |
| 8 | Card rail — mobile | **CONFLICT, resolved**: frozen CSS hides arrows ≤640px; founder override requires one-card-at-a-time, visible centered arrows | Build the override, not the frozen rule (see `01_SOURCE_OF_TRUTH...md` §3.4.2) | 480, 390 | No mobile rail screenshot showing the *target* one-card state exists (only the current hidden-arrow state is implied) | **BLOCKED for visual verification — behavior spec is clear (`INTERACTIONS.md` #4), no reference screenshot of the target state to compare against** |
| 9 | Hero / intro pattern | `.tk-hero.tk-surface-night` → eyebrow → h1 → lede → button row | Gutenberg pattern #1 | 1440, 1024, 768, 480, 390 | Home screenshot | READY |
| 10 | Evidence/stat pattern | `.tk-section.tk-surface-graphite` centered stat + `.tk-evidence-block` claim badge | Gutenberg pattern #2 | Same | Home screenshot ("2.1M tonnes" band) | READY |
| 11 | Narrative zone pattern | Alternating `.tk-surface-night`/`.tk-surface-graphite` eyebrow→h2→lede sections | Gutenberg pattern #3 | Same | **No `our-story.html`/`what-we-build.html` screenshot in Golden Master** — pattern is documented in `PATTERN_MAP.md` #3 but not visually captured | **BLOCKED for visual verification — spec-only** |
| 12 | CTA band (both shapes) | `.tk-closer` (light ask) and `.tk-mission-banner` (heavier, 2-button) | Gutenberg pattern #5, both variants registered | Same | Neither shape has a dedicated screenshot; `.tk-mission-banner` is referenced only in `PATTERN_MAP.md` text | **BLOCKED for visual verification — spec-only** |
| 13 | Need / Work With Us teaser | 3-`.tk-territory-card` shape (Home Support section) + fuller `.tk-contact-grid`+form shape (Contact page) | Gutenberg pattern #6, two variants: homepage teaser vs. full Work With Us / Contact page | Home: screenshotted. Contact: screenshotted (390/768/1440) | Home + Contact screenshots exist | **PARTIAL — both variants have reference imagery, but the 10-route Work With Us page itself (per `RB_WORK_WITH_US_EVIDENCE_ACQUISITION_ARCHITECTURE_v0.1.md`) does not exist yet in the Artifact or as content** |
| 14 | Impact/Update teaser | **GAP** — no dedicated pattern in Artifact | Founder must decide: reuse `.tk-media-card`, or approve a new component | N/A | N/A | **BLOCKED — founder decision required, see `01...md` §3.4.1** |
| 15 | Editorial/policy pattern | `.tk-legal-layout` (TOC sidebar) or `.tk-anchor-nav` (horizontal chips) variants, `.tk-gate-tag` placeholder for unsupplied legal copy | Gutenberg pattern #8 | 1440, 1024, 768, 480, 390 | `terms`/`faq`/`contact` screenshots (390/768/1440 only — 1024/480 missing) | PARTIAL — pattern verified, 2 of 5 required widths missing |
| 16 | Shop landing (Merch/Products) | `.tk-shop-collections` stacked named carousel rows, exactly 2 first-level collections | Woo Shop archive template | All 5 | **No `shop.html` screenshot in Golden Master** | **BLOCKED — no reference imagery at all; build from `COMPONENT_INVENTORY.md` spec + `RB_WEB_UX_NAVIGATION_COLLECTIONS_EDITABILITY_OVERRIDES_v0.1.md` §5, verify once built** |
| 17 | Product detail / gallery | `.tk-gallery` (stage + thumbs + keyboard/swipe nav), `.tk-buy-panel` | Woo single-product template | All 5 | **No `product.html` screenshot** (the `rebuild-material` screenshots are a *different* template — Re:Build material detail, not the generic PDP) | **BLOCKED — no reference imagery for the generic Woo PDP** |
| 18 | Re:Build material detail page | `.tk-hub-head`, breadcrumb, spec/status/evidence layout | Custom Woo product template variant for the 4 Re:Build Products SKUs | All 5 | `rebuild-material` screenshot exists at 390/768/1440 (1024/480 missing) | PARTIAL |
| 19 | Cart/Bag page (full, not drawer) | `.tk-cart-layout`, `.tk-cart-row`, `.tk-order-summary` | Woo cart page | All 5 | **No screenshot** | **BLOCKED** |
| 20 | Checkout | `.tk-checkout-layout`, single-page collapsible steps (`initCollapseGroup`, `openOnly`) | Woo checkout, custom template | All 5 | **No screenshot** | **BLOCKED** |
| 21 | My Account | Quick actions must not be 4 cramped mobile columns (explicit founder complaint from the earlier Home-audit session, carried forward) | Woo My Account, custom template | All 5, mobile especially | **No screenshot** | **BLOCKED** |
| 22 | Footer | `[data-tk-year]` auto-year, legal links, support card | Theme footer template part | All 5 | Visible in every full-page screenshot | READY |

---

## 4. Status summary

- **READY (reference imagery + spec both exist):** Hero/intro, Evidence/stat, general card rail (desktop), footer — 4 of 22 rows.
- **PARTIAL (spec exists, imagery incomplete):** Editorial/policy, Re:Build material detail, Need/Work-With-Us teaser (homepage variant only) — 3 rows.
- **BLOCKED for visual verification (spec-only, no reference screenshot):** Header (both), drawer (both), bag drawer, mobile rail target state, narrative zone, CTA bands, shop landing, generic PDP, cart page, checkout, my account — 13 rows.
- **BLOCKED on founder decision, not imagery:** Impact/Update teaser pattern — 1 row.
- **CONFLICT already resolved in this package (override wins):** mobile rail arrows — 1 row.

13 of 22 rows cannot be visually parity-checked against the Golden Master today because the reference package has no screenshot for that surface. This is the concrete evidence behind blocker B3 in `01_SOURCE_OF_TRUTH_AND_INPUT_STATUS.md` and is one of the two reasons (with content approval) the GO/HOLD call in `09_IMPLEMENTATION_PLAN_4_ITERATIONS.md` is HOLD.
